---
name: using
description: 在任何对话开始时使用——确立如何查找和使用技能，要求在做出任何回复（包括澄清提问）之前必须调用 Skill 工具
---

<SUBAGENT-STOP>
如果你是作为子代理被派遣去执行某个具体任务，请跳过此技能。
</SUBAGENT-STOP>

<EXTREMELY-IMPORTANT>
如果你认为某个技能哪怕只有 1% 的可能性适用于你正在做的事情，你绝对必须调用该技能。

如果某个技能适用于你的任务，你没有选择。你必须使用它。

这不可商量。这不是可选项。你无法通过自我合理化来逃避它。
</EXTREMELY-IMPORTANT>

## 指令优先级

Claude Workflow 技能会覆盖默认系统提示词的行为，但**用户的指令始终优先**：

1. **用户明确写下的指令**（CLAUDE.md、GEMINI.md、AGENTS.md、直接请求）—— 最高优先级
2. **Claude Workflow 技能** —— 在与默认系统行为冲突时覆盖之
3. **默认系统提示词** —— 最低优先级

如果 CLAUDE.md、GEMINI.md 或 AGENTS.md 写着"不要使用 TDD"，而某个技能说"始终使用 TDD"，请遵循用户的指令。用户拥有控制权。

## 如何访问技能

**在 Claude Code 中：** 使用 `Skill` 工具。当你调用一个技能时，其内容会被加载并呈现给你——直接遵循它即可。永远不要用 Read 工具去读技能文件。

**在 Claude Code 之外：** 请查阅你所用平台的文档，了解技能是如何加载的。

# 使用技能

## 规则

**在任何回复或行动之前，先调用相关或被请求的技能。** 即便只有 1% 的可能某个技能适用，你也应当调用该技能去检查。如果调用后发现该技能不适合当前情形，你不必使用它。

### 执行步骤

收到用户消息（包括澄清提问）时：

1. **是否即将进入 PlanMode？**
   - 是 + 本会话未做过 think → 先调用 think 技能
   - 其他情况 → 跳到步骤 2
2. **是否有任何技能 ≥1% 可能适用？**
   - 否 → 直接回复（包括澄清提问）
   - 是 → 进入步骤 3
3. 调用 `Skill` 工具
4. 用户可见回复只说任务动作；不要提 `Skill`、技能名或"加载"，除非用户正在讨论 workflow 本身
5. **技能内是否有 checklist？**
   - 是 → 用 TodoWrite 为每一项建立 todo
   - 否 → 进入步骤 6
6. **严格按技能执行**——不裁剪、不二次解释、不"我觉得这条可以跳过"

### 工作流入口违规对照

| 违规念头 | 实际在做 | 必须改为 |
|---|---|---|
| "这次太简单不需要技能" | 跳过 1% 原则 | 调用技能 |
| "先回复再说，回头补技能" | 偷偷绕过入口 | 先调技能再回复 |
| "用了技能就要说出来" | 暴露 workflow 痕迹 | 只说任务动作，不提技能 |
| "checklist 我记得住" | 用脑子代替工具 | TodoWrite 落地每一项 |
| "这条不太合适，跳过" | 二次解释技能 | 严格执行原文 |
| "只是个澄清问题" | 用"小回复"豁免规则 | 澄清前也要扫技能 |
| "我已经 think 过类似的了" | PlanMode 前置步骤被跳过 | 本会话未做过就要做 |
| "设计已确认，我直接实现" | 把 think 批准误当执行批准 | 进入 plan，先给计划 |
| "只改一个文件不复杂" | 用文件数量代替风险判断 | 先判断影响面，再选计划形式 |
| "命令超时但看起来通过" | 把不完整证据当完成 | 调用 verify，报告真实状态 |

### 正反例

**正例**

```
用户："帮我重构这个函数"
你：我先厘清改动范围，确认后再动代码。
    [按技能步骤执行]
```

```
用户："今天巴黎天气怎么样"
你：[扫描技能列表 → 无相关技能]
    [直接回复天气]
```

**反例**（不要这样做）

```
用户："帮我重构这个函数"
你："好的，我先看下代码……"
                            ↑ 错：跳过技能扫描，直接动手
```

```
用户："进入 plan mode 写个登录功能"
你：[直接 EnterPlanMode]
                            ↑ 错：未先调用 think
```

```
用户："加一个测试"
你："我来写测试……"
    [此时还没加载适用技能]
                            ↑ 错：先回复后加载流程
```

### 边界澄清

- **澄清提问也算回复** → 提问前同样要扫技能
- **空消息 / 纯打招呼**（"在吗"）→ 无技能适用，直接回复
- **多个技能同时适用** → 全部调用，先处理最相关的；用户可见回复仍只说任务动作
- **不确定哪个技能适用** → 宁可多检查一次，用 1% 原则处理
- **完成声明前** → 必须调用 verify；timeout、截断输出、无退出码都不是通过证据

## 危险信号

以下念头意味着停下——你正在自我合理化：

| 念头 | 现实 |
|---------|---------|
| "这只是个简单问题" | 提问就是任务。检查是否有技能。 |
| "我需要先获取更多上下文" | 检查技能要在澄清提问之前。 |
| "让我先探索一下代码库" | 技能会告诉你如何探索。先检查。 |
| "我可以快速看一下 git/文件" | 文件缺少对话上下文。检查是否有技能。 |
| "我先收集一下信息" | 技能会告诉你如何收集信息。 |
| "这不需要一个正式的技能" | 如果存在技能，就使用它。 |
| "我记得这个技能" | 技能会演进。读当前版本。 |
| "这不算一个任务" | 行动即任务。检查是否有技能。 |
| "用这个技能太过了" | 简单的事情会变复杂。用它。 |
| "我就先做这一件事" | 在做任何事之前先检查。 |
| "这感觉很有效率" | 无纪律的行动浪费时间。技能避免这一点。 |
| "我知道那是什么意思" | 知道概念 ≠ 使用技能。调用它。 |

## 技能优先级

当多个技能都可能适用时，按以下顺序使用：

1. **先用流程类技能**（think、debug）——这些决定**如何**处理任务
2. **再用实现类或项目技能** —— 这些指导执行

"我们来构建 X" → 先 think，然后是实现类技能。
"修复这个 bug" → 先 debug，然后是领域相关的技能。

think 确认的是方向；plan 确认的是执行步骤。用户没有明确说"直接实现/继续写代码"时，plan 之后必须等执行确认。

## 技能类型

**刚性**（TDD、debug）：严格遵循。不要把纪律性变通掉。

**柔性**（模式类）：将原则适配到上下文。

技能本身会告诉你属于哪一类。

## 用户指令

指令说的是"做什么"，不是"怎么做"。"添加 X"或"修复 Y"并不意味着跳过工作流。
