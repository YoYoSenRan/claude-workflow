{
  "name": "claude-workflow",
  "private": true,
  "description": "Chinese-first Claude Code workflow skills, routing rules, and SessionStart hook.",
  "repository": {
    "type": "git",
    "url": "https://github.com/YoYoSenRan/claude-workflow.git"
  },
  "license": "MIT",
  "scripts": {
    "dev": "claude --plugin-dir .",
    "test": "bash tests/run-all.sh --skip-triggering",
    "test:hook": "bash tests/hooks/test-session-start.sh",
    "test:plugin": "bash tests/plugin/test-validate.sh",
    "test:triggering": "bash tests/skill-triggering/run-all.sh",
    "test:all": "bash tests/run-all.sh",
    "validate": "claude plugin validate .claude-plugin/plugin.json && claude plugin validate .claude-plugin/marketplace.json && node hooks/session-start.js",
    "install:dev": "claude plugin uninstall claude-workflow@claude-workflow --scope user --keep-data || true && claude plugin marketplace remove claude-workflow || true && claude plugin marketplace add ./ --scope user && claude plugin install claude-workflow@claude-workflow --scope user",
    "install:github": "claude plugin marketplace add https://github.com/YoYoSenRan/claude-workflow.git --scope user && claude plugin install claude-workflow@claude-workflow --scope user",
    "update": "claude plugin marketplace update claude-workflow && claude plugin update claude-workflow@claude-workflow --scope user",
    "uninstall": "claude plugin uninstall claude-workflow@claude-workflow --scope user && claude plugin marketplace remove claude-workflow",
    "plugin:list": "claude plugin list"
  }
}
